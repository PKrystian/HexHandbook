name: React Native CI/CD

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Install dependencies
      run: npm install

    - name: Run unit tests
      run: npm test || true

    - name: Build and test Android
      # run: TODO add tests for Android

    - name: Build and test iOS
      # run: TODO add tests for iOS

    - name: Build and test Web
      # run: TODO add tests for Web

    - name: Check code structure and readability
      run: npm run lint

    - name: React Project Checker
      uses: VictorDanilov/react-project-checker-action@v0.0.1-alpha
